import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'partymeister-companion-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
